# Timestamp-It

A Python project that finds where a quote or phrase occurs in a video and returns the timestamp.

## Features

- Video transcription
- Text search
- Timestamp retrieval
- Notebook-based workflow

## Usage

Open `draft_timestamp_it.ipynb` and run the cells.

## Requirements

See `requirements.txt`.
